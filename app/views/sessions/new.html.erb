<%= form_tag('/signin') do %>

    <p>Select a User:</p>
    <%= collection_select(:user, :name, User.all, :name, :name) %><br />
    <p>Password: <%= password_field_tag :password %></p>
    <%= submit_tag "Sign In" %>
  
<% end %>